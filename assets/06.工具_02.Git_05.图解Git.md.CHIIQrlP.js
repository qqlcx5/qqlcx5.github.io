import{bV as a,aL as o,u as t,G as c}from"./chunks/framework.DvBUyM1S.js";const m=JSON.parse('{"title":"图解Git","description":"","frontmatter":{"title":"图解Git","date":"2022-05-10T23:30:59.000Z","permalink":"/pages/20730e/","categories":[null],"tags":["Git"]},"headers":[],"relativePath":"06.工具/02.Git/05.图解Git.md","filePath":"06.工具/02.Git/05.图解Git.md","lastUpdated":1652233370000}'),i={name:"06.工具/02.Git/05.图解Git.md"};function r(s,e,l,n,d,g){return o(),t("div",null,[...e[0]||(e[0]=[c('<h1 id="图解git" tabindex="-1">图解Git <a class="header-anchor" href="#图解git" aria-label="Permalink to &quot;图解Git&quot;">​</a></h1><blockquote><p>来源：<a href="http://marklodato.github.io/visual-git-guide/index-zh-cn.html" target="_blank" rel="noreferrer">http://marklodato.github.io/visual-git-guide/index-zh-cn.html</a></p></blockquote><h2 id="基本用法" tabindex="-1">基本用法 <a class="header-anchor" href="#基本用法" aria-label="Permalink to &quot;基本用法&quot;">​</a></h2><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/basic-usage.svg" alt="" loading="lazy"></p><p>上面的四条命令在工作目录、暂存目录(也叫做索引)和仓库之间复制文件。</p><ul><li><code>git add _files_</code> 把当前文件放入暂存区域。</li><li><code>git commit</code> 给暂存区域生成快照并提交。</li><li><code>git reset -- _files_</code> 用来撤销最后一次<code>git add _files_</code>，你也可以用<code>git reset</code> 撤销所有暂存区域文件。</li><li><code>git checkout -- _files_</code> 把文件从暂存区域复制到工作目录，用来丢弃本地修改。</li></ul><p>你可以用 <code>git reset -p</code>, <code>git checkout -p</code>, or <code>git add -p</code>进入交互模式。</p><p>也可以跳过暂存区域直接从仓库取出文件或者直接提交代码。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/basic-usage-2.svg" alt="" loading="lazy"></p><ul><li><code>git commit -a</code> 相当于运行 git add 把所有当前目录下的文件加入暂存区域再运行。git commit.</li><li><code>git commit _files_</code> 进行一次包含最后一次提交加上工作目录中文件快照的提交。并且文件被添加到暂存区域。</li><li><code>git checkout HEAD -- _files_</code> 回滚到复制最后一次提交。</li></ul><h2 id="约定" tabindex="-1">约定 <a class="header-anchor" href="#约定" aria-label="Permalink to &quot;约定&quot;">​</a></h2><p>后文中以下面的形式使用图片。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/conventions.svg" alt="" loading="lazy"></p><p>绿色的5位字符表示提交的ID，分别指向父节点。分支用橘色显示，分别指向特定的提交。当前分支由附在其上的_HEAD_标识。 这张图片里显示最后5次提交，_ed489_是最新提交。 _main_分支指向此次提交，另一个_stable_分支指向祖父提交节点。</p><h2 id="命令详解" tabindex="-1">命令详解 <a class="header-anchor" href="#命令详解" aria-label="Permalink to &quot;命令详解&quot;">​</a></h2><h3 id="diff" tabindex="-1">Diff <a class="header-anchor" href="#diff" aria-label="Permalink to &quot;Diff&quot;">​</a></h3><p>有许多种方法查看两次提交之间的变动。下面是一些示例。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/diff.svg" alt="" loading="lazy"></p><h3 id="commit" tabindex="-1">Commit <a class="header-anchor" href="#commit" aria-label="Permalink to &quot;Commit&quot;">​</a></h3><p>提交时，git用暂存区域的文件创建一个新的提交，并把此时的节点设为父节点。然后把当前分支指向新的提交节点。下图中，当前分支是_main_。 在运行命令之前，<em>main_指向_ed489</em>，提交后，_main_指向新的节点_f0cec_并以_ed489_作为父节点。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/commit-main.svg" alt="" loading="lazy"></p><p>即便当前分支是某次提交的祖父节点，git会同样操作。下图中，在_main_分支的祖父节点_stable_分支进行一次提交，生成了_1800b_。 这样，_stable_分支就不再是_main_分支的祖父节点。此时，<a href="about:blank#merge" target="_blank" rel="noreferrer">合并</a> (或者 <a href="about:blank#rebase" target="_blank" rel="noreferrer">衍合</a>) 是必须的。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/commit-stable.svg" alt="" loading="lazy"></p><p>如果想更改一次提交，使用 <code>git commit --amend</code>。git会使用与当前提交相同的父节点进行一次新提交，旧的提交会被取消。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/commit-amend.svg" alt="" loading="lazy"></p><p>另一个例子是<a href="about:blank#detached" target="_blank" rel="noreferrer">分离HEAD提交</a>,后文讲。</p><h3 id="checkout" tabindex="-1">Checkout <a class="header-anchor" href="#checkout" aria-label="Permalink to &quot;Checkout&quot;">​</a></h3><p>checkout命令用于从历史提交（或者暂存区域）中拷贝文件到工作目录，也可用于切换分支。</p><p>当给定某个文件名（或者打开-p选项，或者文件名和-p选项同时打开）时，git会从指定的提交中拷贝文件到暂存区域和工作目录。比如，<code>git checkout HEAD~ foo.c</code>会将提交节点_HEAD~_(即当前提交节点的父节点)中的<code>foo.c</code>复制到工作目录并且加到暂存区域中。（如果命令中没有指定提交节点，则会从暂存区域中拷贝内容。）注意当前分支不会发生变化。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/checkout-files.svg" alt="" loading="lazy"></p><p>当不指定文件名，而是给出一个（本地）分支时，那么_HEAD_标识会移动到那个分支（也就是说，我们“切换”到那个分支了），然后暂存区域和工作目录中的内容会和_HEAD_对应的提交节点一致。新提交节点（下图中的a47c3）中的所有文件都会被复制（到暂存区域和工作目录中）；只存在于老的提交节点（ed489）中的文件会被删除；不属于上述两者的文件会被忽略，不受影响。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/checkout-branch.svg" alt="" loading="lazy"></p><p>如果既没有指定文件名，也没有指定分支名，而是一个标签、远程分支、SHA-1值或者是像_main~3_类似的东西，就得到一个匿名分支，称作_detached HEAD_（被分离的_HEAD_标识）。这样可以很方便地在历史版本之间互相切换。比如说你想要编译1.6.6.1版本的git，你可以运行<code>git checkout v1.6.6.1</code>（这是一个标签，而非分支名），编译，安装，然后切换回另一个分支，比如说<code>git checkout main</code>。然而，当提交操作涉及到“分离的HEAD”时，其行为会略有不同，详情见在<a href="about:blank#detached" target="_blank" rel="noreferrer">下面</a>。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/checkout-detached.svg" alt="" loading="lazy"></p><h3 id="head标识处于分离状态时的提交操作" tabindex="-1">HEAD标识处于分离状态时的提交操作 <a class="header-anchor" href="#head标识处于分离状态时的提交操作" aria-label="Permalink to &quot;HEAD标识处于分离状态时的提交操作&quot;">​</a></h3><p>当_HEAD_处于分离状态（不依附于任一分支）时，提交操作可以正常进行，但是不会更新任何已命名的分支。(你可以认为这是在更新一个匿名分支。)</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/commit-detached.svg" alt="" loading="lazy"></p><p>一旦此后你切换到别的分支，比如说_main_，那么这个提交节点（可能）再也不会被引用到，然后就会被丢弃掉了。注意这个命令之后就不会有东西引用_2eecb_。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/checkout-after-detached.svg" alt="" loading="lazy"></p><p>但是，如果你想保存这个状态，可以用命令<code>git checkout -b _name_</code>来创建一个新的分支。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/checkout-b-detached.svg" alt="" loading="lazy"></p><h3 id="reset" tabindex="-1">Reset <a class="header-anchor" href="#reset" aria-label="Permalink to &quot;Reset&quot;">​</a></h3><p>reset命令把当前分支指向另一个位置，并且有选择的变动工作目录和索引。也用来在从历史仓库中复制文件到索引，而不动工作目录。</p><p>如果不给选项，那么当前分支指向到那个提交。如果用<code>--hard</code>选项，那么工作目录也更新，如果用<code>--soft</code>选项，那么都不变。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/reset-commit.svg" alt="" loading="lazy"></p><p>如果没有给出提交点的版本号，那么默认用_HEAD_。这样，分支指向不变，但是索引会回滚到最后一次提交，如果用<code>--hard</code>选项，工作目录也同样。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/reset.svg" alt="" loading="lazy"></p><p>如果给了文件名(或者 <code>-p</code>选项), 那么工作效果和带文件名的<a href="about:blank#checkout" target="_blank" rel="noreferrer">checkout</a>差不多，除了索引被更新。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/reset-files.svg" alt="" loading="lazy"></p><h3 id="merge" tabindex="-1">Merge <a class="header-anchor" href="#merge" aria-label="Permalink to &quot;Merge&quot;">​</a></h3><p>merge 命令把不同分支合并起来。合并前，索引必须和当前提交相同。如果另一个分支是当前提交的祖父节点，那么合并命令将什么也不做。 另一种情况是如果当前提交是另一个分支的祖父节点，就导致_fast-forward_合并。指向只是简单的移动，并生成一个新的提交。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/merge-ff.svg" alt="" loading="lazy"></p><p>否则就是一次真正的合并。默认把当前提交(<em>ed489</em> 如下所示)和另一个提交(<em>33104</em>)以及他们的共同祖父节点(<em>b325c</em>)进行一次<a href="http://en.wikipedia.org/wiki/Three-way_merge" target="_blank" rel="noreferrer">三方合并</a>。结果是先保存当前目录和索引，然后和父节点_33104_一起做一次新提交。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/merge.svg" alt="" loading="lazy"></p><h3 id="cherry-pick" tabindex="-1">Cherry Pick <a class="header-anchor" href="#cherry-pick" aria-label="Permalink to &quot;Cherry Pick&quot;">​</a></h3><p>cherry-pick命令&quot;复制&quot;一个提交节点并在当前分支做一次完全一样的新提交。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/cherry-pick.svg" alt="" loading="lazy"></p><h3 id="rebase" tabindex="-1">Rebase <a class="header-anchor" href="#rebase" aria-label="Permalink to &quot;Rebase&quot;">​</a></h3><p>衍合是合并命令的另一种选择。合并把两个父分支合并进行一次提交，提交历史不是线性的。衍合在当前分支上重演另一个分支的历史，提交历史是线性的。 本质上，这是线性化的自动的 <a href="about:blank#cherry-pick" target="_blank" rel="noreferrer">cherry-pick</a></p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/rebase.svg" alt="" loading="lazy"></p><p>上面的命令都在_topic_分支中进行，而不是_main_分支，在_main_分支上重演，并且把分支指向新的节点。注意旧提交没有被引用，将被回收。</p><p>要限制回滚范围，使用<code>--onto</code>选项。下面的命令在_main_分支上重演当前分支从_169a6_以来的最近几个提交，即_2c33a_。</p><p><img src="https://feyoudao.oss-cn-hongkong.aliyuncs.com/mweb/rebase-onto.svg" alt="" loading="lazy"></p><p>同样有<code>git rebase --interactive</code>让你更方便的完成一些复杂操作，比如丢弃、重排、修改、合并提交。没有图片体现这些，细节看这里:<a href="http://www.kernel.org/pub/software/scm/git/docs/git-rebase.html#_interactive_mode" target="_blank" rel="noreferrer">git-rebase(1)</a></p><h2 id="技术说明" tabindex="-1">技术说明 <a class="header-anchor" href="#技术说明" aria-label="Permalink to &quot;技术说明&quot;">​</a></h2><p>文件内容并没有真正存储在索引(<em>.git/index</em>)或者提交对象中，而是以blob的形式分别存储在数据库中(<em>.git/objects</em>)，并用SHA-1值来校验。 索引文件用识别码列出相关的blob文件以及别的数据。对于提交来说，以树(<em>tree</em>)的形式存储，同样用对于的哈希值识别。树对应着工作目录中的文件夹，树中包含的 树或者blob对象对应着相应的子目录和文件。每次提交都存储下它的上一级树的识别码。</p><p>如果用detached HEAD提交，那么最后一次提交会被the reflog for HEAD引用。但是过一段时间就失效，最终被回收，与<code>git commit --amend</code>或者<code>git rebase</code>很像。</p>',67)])])}const p=a(i,[["render",r]]);export{m as __pageData,p as default};
